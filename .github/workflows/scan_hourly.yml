# RETIRED — replaced by scan_h1.yml
# Kept for emergency manual use only. Schedule removed.
name: Hourly Regime Check (Retired)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scan_hourly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run hourly regime check
        env:
          TWELVEDATA_KEY:     ${{ secrets.TWELVEDATA_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:   ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python -m scanner.scan_hourly

      - name: Commit data
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/signals.json
          git diff --cached --quiet || git commit -m "chore: hourly $(date -u +%Y-%m-%dT%H:%M)"
          git pull --rebase origin main
          git push
